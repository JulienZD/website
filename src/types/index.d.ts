export interface ColorCombination {
  primary: string;
  secondary: string;
  contrast: number;
}

export interface ProjectMeta {
  title: string;
  description: string;
  image: string;
  identifier: string;
  linkText?: string;
}
